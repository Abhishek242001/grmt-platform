fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
cryptography==43.0.1
argon2-cffi==23.1.0
python-multipart==0.0.12
httpx==0.27.2
python-docx==1.1.2
PyMuPDF==1.28.2
psycopg2-binary==2.9.9
pytest==8.3.3
pytest-asyncio==0.24.0

# AI-generated-text detection (Binoculars) — deliberately NOT pinning torch
# here. Lightning Studio GPU images come with a CUDA-matched torch
# preinstalled; a generic pip-pinned torch version risks breaking that
# careful CUDA match. Confirm `torch.cuda.is_available()` returns True
# BEFORE installing anything else — see PROJECT_HANDOFF.md §7.2c.
transformers>=4.46.0
accelerate>=1.0.0
